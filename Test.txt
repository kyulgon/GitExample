Test github


pull 
1. git init
2. git remote add origin 주소
3. git pull origin master

push
1. git status
2. git add .
3. git commit -m "message"
4. git push origin master

pull 전에 pull가 있다. git pull origin master
